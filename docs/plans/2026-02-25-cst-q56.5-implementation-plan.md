# CST q56.5 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Introduce a dedicated CST package with typed concrete node contracts and migrate parser CST output to use it while preserving parser behavior.

**Architecture:** Add `src/cst` for concrete syntax ownership. Update parser contracts to reference `@cst` types and migrate parser CST construction to typed nodes with preserved token slices/spans/trivia.

**Tech Stack:** MoonBit, existing scanner/parser/ast packages, MoonSpec tests, file-based mise tasks.

---

### Task 1: Add CST package skeleton and root compile checks

**Files:**
- Create: `src/cst/moon.pkg`
- Create: `src/cst/types.mbt`
- Modify: `src/moon.pkg`
- Modify: `src/lib.mbt`
- Modify: `src/lib_test.mbt`

**Steps:**
1. Add a failing root-level type test expecting CST types from root exports.
2. Run `moon test src --target js` and verify RED.
3. Create minimal CST package skeleton and root imports/re-exports.
4. Re-run root tests to GREEN.
5. Commit.

### Task 2: Define CST contracts with package tests

**Files:**
- Modify: `src/cst/types.mbt`
- Create: `src/cst/types_test.mbt`

**Steps:**
1. Add failing CST tests for `ModuleCst`, header/import nodes, and typed declaration enum variants.
2. Run `moon test src/cst --target js` and verify RED.
3. Implement CST contracts (`ModuleCst`, `ModuleHeaderCst`, `ImportCst`, `DeclarationCst`, `FunctionCst`, `TypeAliasCst`, `UnionTypeCst`).
4. Re-run CST tests to GREEN.
5. Commit.

### Task 3: Migrate parser contracts to CST package ownership

**Files:**
- Modify: `src/parser/moon.pkg`
- Modify: `src/parser/types.mbt`
- Modify: `src/parser/types_test.mbt`

**Steps:**
1. Add failing parser type tests expecting `ParseResult.cst : @cst.ModuleCst?`.
2. Run `moon test src/parser --target js` and verify RED.
3. Update parser contract types to remove parser-owned CST definitions and reference `@cst`.
4. Re-run parser type tests to GREEN.
5. Commit.

### Task 4: Migrate parser CST construction to typed nodes

**Files:**
- Modify: `src/parser/lib.mbt`
- Modify: `src/parser/lib_test.mbt`
- Modify: `src/bdd/parser_world_wbtest.mbt` (if CST assertions need adjustment)
- Modify: `src/lib_test.mbt` (if root CST assertions need adjustment)

**Steps:**
1. Add/adjust parser behavior tests for typed CST variants and token-slice/span fidelity.
2. Run `moon test src/parser --target js` and verify RED.
3. Update parser implementation to emit typed CST nodes for header/import/declarations.
4. Re-run parser and root tests to GREEN.
5. Commit.

### Task 5: Generated interfaces and full verification

**Files:**
- Modify: `src/lib.mbt` (if needed)
- Modify: `src/pkg.generated.mbti` (generated)
- Create/Modify: `src/cst/pkg.generated.mbti` (generated)
- Modify: `src/parser/pkg.generated.mbti` (generated)

**Steps:**
1. Run `moon info`.
2. Run `moon fmt`.
3. Run `mise run check`.
4. Run `mise run coverage:ci-gate`.
5. Commit.

### Task 6: Issue and branch completion

**Files:**
- Modify: `.beads/issues.jsonl` (auto-managed)

**Steps:**
1. Close issue: `bd close krueger-q56.5 --reason "Completed" --json`.
2. Push branch.
3. Open/update PR.
