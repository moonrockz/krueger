# AST q56.4 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Introduce a dedicated AST package with domain-rich types and migrate parser AST output to use it while preserving parser behavior.

**Architecture:** Add `src/ast` for semantic model ownership. Update parser contracts to reference `@ast` types and migrate parser construction logic to typed wrappers with hybrid expression/type fallback nodes. Keep CST in parser for q56.5.

**Tech Stack:** MoonBit, existing scanner/parser packages, MoonSpec tests, file-based mise tasks.

---

### Task 1: Add AST package skeleton and failing compile checks

**Files:**
- Create: `src/ast/moon.pkg`
- Create: `src/ast/types.mbt`
- Modify: `src/moon.pkg`
- Modify: `src/lib.mbt`
- Modify: `src/lib_test.mbt`

**Steps:**
1. Add root compile-level test references expecting AST types from root exports.
2. Run `moon test src --target js` and verify RED.
3. Create minimal AST package skeleton and root imports/re-exports.
4. Re-run tests to GREEN.
5. Commit.

### Task 2: Define AST domain and declaration/expr/type contracts with type tests

**Files:**
- Modify: `src/ast/types.mbt`
- Create: `src/ast/types_test.mbt`

**Steps:**
1. Add failing tests for domain wrappers and hybrid expr/type nodes.
2. Run `moon test src/ast --target js` and verify RED.
3. Implement AST contracts (`Identifier`, name wrappers, `ModuleAst`, declarations, `Expr`, `TypeExpr`, `Unparsed*`).
4. Re-run AST tests to GREEN.
5. Commit.

### Task 3: Migrate parser types to reference AST package

**Files:**
- Modify: `src/parser/moon.pkg`
- Modify: `src/parser/types.mbt`
- Modify: `src/parser/types_test.mbt`

**Steps:**
1. Add failing parser type tests expecting `ParseResult.ast` as AST package type and typed declaration payloads.
2. Run `moon test src/parser --target js` and verify RED.
3. Update parser type contracts to remove parser-owned AST structures and use `@ast` types.
4. Re-run parser type tests to GREEN.
5. Commit.

### Task 4: Migrate parser construction logic to typed AST wrappers

**Files:**
- Modify: `src/parser/lib.mbt`
- Modify: `src/parser/lib_test.mbt`
- Modify: `src/lib_test.mbt`

**Steps:**
1. Add/adjust parser behavior tests to assert wrapped names and hybrid body nodes.
2. Run `moon test src/parser --target js` and verify RED.
3. Update parser implementation to construct typed AST nodes and fallback `Unparsed` variants.
4. Re-run parser and root tests to GREEN.
5. Commit.

### Task 5: Final exports, generated interfaces, and verification

**Files:**
- Modify: `src/lib.mbt` (if needed)
- Modify: `src/pkg.generated.mbti` (generated)
- Create/Modify: `src/ast/pkg.generated.mbti` (generated)
- Modify: `src/parser/pkg.generated.mbti` (generated)

**Steps:**
1. Run `moon info`.
2. Run `moon fmt`.
3. Run `mise run check`.
4. Run `mise run coverage:ci-gate`.
5. Commit.

### Task 6: Issue and branch completion

**Files:**
- Modify: `.beads/issues.jsonl` (auto-managed)

**Steps:**
1. Close issue: `bd close krueger-q56.4 --reason "Completed" --json`.
2. Push branch.
3. Update/open PR as needed.
